# Interactive 3D Typography Sphere

## Install
npm i three @react-three/fiber gsap

## Files
- components/TextSphere.jsx — the whole effect (client component)
- app/page.jsx — mounts it full-screen

Tailwind must be configured to scan ./components/**/*.{js,jsx}.
